#include <bits/stdc++.h>

const int N = 505;
int h[N];

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  int n, m;
  std::cin >> n >> m;
  for(int i = 1; i <= n; i++) std::cin >> h[i];

  
  return 0;
}