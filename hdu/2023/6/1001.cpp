#include<bits/stdc++.h>
typedef long long ll;
using namespace std;

const ll mod = 998244353;

void solve() {
  ll n, m, k, ans;
  cin >> n >> m >> k;
  if(n >= 2 * k) {
    ans = 1;
    
  }
}

int main(){
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int t;
  cin >> t;
  while(t--) {
    solve();
  }
  return 0;
}