#include<bits/stdc++.h>
using namespace std;

const int N = 2e5 + 5; 
int l[N], r[N];

int main(){
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int n, m, q;
  string s;
  cin >> n >> m >> q;
  cin >> s;
  
  for(int i = 1; i <= m; i++) 
    cin >> l[i] >> r[i];
  for(int i = 1; i <= q; i++) {
    int x;
    cin >> x;

  }
  return 0;
}