#include<bits/stdc++.h>
typedef long long ll;
using namespace std;

const int N = 1e5 + 5;
int a[15][N];

int main(){
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int n, m, k;
  cin >> n >> m >> k;
  for(int i = 1; i <= n; i++) {
    for(int j = 1; j <= m; j++) 
      cin >> a[i][j]; 
  } 
  for(int i = 1; i <= n; i++) {
    
  }
  return 0;
}