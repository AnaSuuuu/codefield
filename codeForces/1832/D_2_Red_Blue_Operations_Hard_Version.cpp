#include <bits/stdc++.h>

const int N = 1005;
int a[N];

int main() {
  int n, q;
  std::cin >> n >> q;
  for(int i = 1; i <= n; i++) std::cin >> a[i];
  for(int i = 1; i <= q; i++) {
    int k;
    std::cin >> k;
    if(k <= n) {
      
    }
    else {
      
    }
  }
  return 0;
}